/** Dispatched after programmatic navigation clears `#article-*` so the category shell can re-read `location.hash`. */
export const KNOWLEDGE_CATEGORY_HASH_SYNC_EVENT = "knowledge-category-hash-sync";
