'use client';

interface ChatButtonProps {
  onClick: () => void;
  isLoaded: boolean;
}

const ChatButton: React.FC<ChatButtonProps> = ({
  onClick: _onClick,
  isLoaded: _isLoaded,
}) => {
  return null;
};

export default ChatButton;
